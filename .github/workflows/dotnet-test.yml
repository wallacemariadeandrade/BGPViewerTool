name: .NET

on:
  push:
    branches: [ master ]
    paths: 
      - BGPViewerCore/*
  pull_request:
    branches: [ master ]
    paths: 
      - BGPViewerCore/*

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Install Google Chrome # Using shell script to install Google Chrome
      run: |
       chmod +x .BGPViewerTool/scripts/InstallChrome.sh
        .BGPViewerTool/scripts/InstallChrome.sh
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Restore dependencies
      run: dotnet restore
    - name: Test
      run: dotnet test --verbosity normal
